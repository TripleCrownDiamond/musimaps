# Musimaps Web

Client web React + Vite de Musimaps.

Le catalogue d’artistes, les villes et les types métier proviennent de
`@musimaps/shared`. Les migrations du backend commun sont à la racine dans
`supabase/migrations`.

Depuis la racine du monorepo :

```bash
npm run dev:web
npm run build:web
```
